📏 The Math (very simple)

Linear regression equation:
y = m × x + b
m = slope (how steep the line is)
b = intercept (where line starts)
x = input
y = predicted value

But don’t worry — sklearn calculates m and b automatically.

If outcome is a number, linear regression is used.